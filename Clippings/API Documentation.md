---
title: "API Documentation"
source: "https://nano-gpt.com/api"
author:
published:
created: 2026-09-03
description: "OpenAI-compatible API for NanoGPT. Streaming, tool calling, and 1,000 text, image, video, audio, speech, and embedding models. Text models billed at list prices with no markup, with the exact cost returned on every chat response."
tags:
  - "clippings"
---
## NanoGPT API Documentation

1081 Total Models

608 Text Models

207 Image Models

155 Video Models

67 Audio TTS

17 STT Models

27 Embedding Models

### Start with the OpenAI-compatible API

Point existing clients at NanoGPT, keep your current request shape, and switch models without rewriting integrations.

Drop-in base URL

Use the official OpenAI SDK with a different base URL.

`https://nano-gpt.com/api/v1`

1,081 models in one catalog

608 text, 207 image, 155 video, plus audio, speech, and embeddings.

Spend and privacy controls

Put limits on keys, use PII redaction where needed, and keep pay-as-you-go billing in the same wallet.

### API Keys

Manage your API authentication

You can create up to 20 API keys. Need more? [Contact support](mailto:support@nano-gpt.com)

AutoGenerated

Active

Created: Sep 3, 2026

Last used: Never used

sk-n...036c

teste01

Active

Created: Sep 3, 2026

Last used: Never used

sk-n...dba3

Base URL

https://nano-gpt.com/api/v1

## Quick Start

Get up and running in minutes

1

##### Create API Key

Generate your API key above

2

##### Use Our Examples

Copy code examples in Python, JavaScript, or cURL

3

##### Make Requests

Start using 1,081 AI models instantly

##### Try it with cURL

Copy and run these examples in your terminal. Copying an example includes the API key.

```bash
curl -X POST https://nano-gpt.com/api/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.2",
    "messages": [
      { "role": "user", "content": "Hello, how are you?" }
    ]
  }'
```

Note: when you use reasoning (thinking) models, some client software, such as LiteLLM, might fail to process reasoning content. In this case, try the [https://nano-gpt.com/api/v1legacy](https://nano-gpt.com/api/v1legacy) endpoint instead of [https://nano-gpt.com/api/v1](https://nano-gpt.com/api/v1). For more information, refer to the [documentation](https://docs.nano-gpt.com/).

Accountless x402 requests return a live 402 quote instead of asking for an API key, card, dashboard, or pre-funded balance.

One unauthenticated request

Supported endpoints return `402 Payment Required` with `payment.accepted[]`.

Nano and Base USDC

Use quote-and-complete rails or exact `X-PAYMENT` replay when a wallet signs the payment payload.

Stable matrix

`GET /api/v1/x402/endpoints` lists only the accountless contract enabled in the current deployment.

```
curl -i https://nano-gpt.com/api/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4.1-nano","messages":[{"role":"user","content":"Say ok"}],"stream":false}'
```

### Documentation

Complete API reference and guides

#### Full API Documentation

For comprehensive API documentation, integration guides, and best practices, visit our documentation site.

[Visit docs.nano-gpt.com](https://docs.nano-gpt.com/)

### API Endpoints

Available endpoints and their capabilities

Chat Completions

`POST /api/v1/chat/completions`

OpenAI-compatible endpoint for text generation with streaming support

Image Generation

`POST /api/generate-image`

Generate images with DALL-E, Flux, and more

Video Generation

`POST /api/generate-video`

Create videos with Kling, Veo, Hunyuan, and other providers. This endpoint is asynchronous: it returns a runId and requires polling `/api/generate-video/status` (include `modelSlug`) until completed to obtain the final video URL.

Audio & Speech

`POST /api/text-to-speech`

Text-to-speech and speech-to-text capabilities

Models List

`GET /api/v1/models`

Get available models with pricing information

For the full endpoint list, see our [Documentation](https://docs.nano-gpt.com/)

### Available Models

Browse and search our model catalog. Prefer a dedicated browser? Use [/models](https://nano-gpt.com/models).

Model catalog

The full searchable catalog loads when you get to it, keeping the API setup view fast.

### Code Examples

Get started with code samples in multiple languages

Code examples

Language-specific examples load when you reach this section.

### Pricing

Simple, transparent pricing for all models

All prices are pay-as-you-go with no monthly fees. Prices shown are API prices per 1 million tokens for text models.

[View Full Pricing](https://nano-gpt.com/pricing)

For bulk discounts, please [contact our sales team](mailto:support@nano-gpt.com) or visit our [Discord community](https://discord.gg/KaQt8gPG6V).

### MCP

Use NanoGPT via MCP-compatible clients

Connect NanoGPT to tools like Claude Code and Cursor using the Model Context Protocol (MCP).

[Open MCP Docs](https://docs.nano-gpt.com/integrations/mcp)

### NanoGPT Onion service

Tor

Access the authenticated text API directly as a Tor Onion service.

Onion API base URL

`http://nanogpt5wwal7shnauuilouj3gaewmk7lskqxdmxlbfz5ho4tdqeh2id.onion/api/v1`

Onion services use `http://` because Tor itself authenticates and encrypts the connection. Copy the address only from an official NanoGPT page.

Resolve through Tor

Use `--socks5-hostname` so cURL sends the Onion hostname through the Tor proxy instead of trying local DNS.

Your regular API key

Use the same NanoGPT API key and request formats. Free models, media, batches, account routes, and the web app are not exposed here.

##### cURL through a local Tor proxy

Start Tor locally first. Its SOCKS port is commonly `9050`; change this to `9150` when using Tor Browser's proxy.

```bash
curl --socks5-hostname 127.0.0.1:9050 \
  -X POST http://nanogpt5wwal7shnauuilouj3gaewmk7lskqxdmxlbfz5ho4tdqeh2id.onion/api/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.2",
    "messages": [{ "role": "user", "content": "Hello through Tor" }]
  }'
```