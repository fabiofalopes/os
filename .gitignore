# Obsidian workspace state
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Harness runtime state (not version-controlled)
_harness/state/
_harness/*.lock
_harness/secrets.env
_harness/lusofona.env

# Sync engine runtime state + bisync backups (not version-controlled)
_sync/state/
_sync/backups/

# One-off large backup + gateway hold markers (runtime state)
bees-backup-*.zip
_harness/.gateway_*

# Secrets never enter the repo (mirror lessons, 2026-09-03)
*.env
wiki/import/agent-knowledge/

# Large pulled snapshots/repos — reproducible from source (e.g. Software Heritage), not git-tracked
projects/trading-agents/repos/

# OS cruft
.DS_Store
Thumbs.db
