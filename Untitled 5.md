
```shell
export HOME=/Users/csi # otherwise ~ resolves into the sandbox
pi -p --mode json --no-session --name research-representation --tools vault,read,bash "Research <directive>. Capture ≤3 atomic claims: vault action=capture zone=inbox origin=agent"
```

