---
id: CLM-0011
type: claim
grade: 4
status: verified
first_seen: 2026-08-26
last_confirmed: 2026-08-26
versions: ["2.1.91", "2.1.196"]
sources: [SRC-0004]
experiments: [EXP-0001, EXP-0002]
superseded_by: null
summary: DEMONSTRATED (full chain): 2.1.91 AND 2.1.196 (native binary, byte-equivalent) obfuscated proxy/lab detection (147-host blocklist, 11 CN-lab keywords, CN-TZ check) encoded as apostrophe variants in the currentDate line, injected via Bg8 into the FIRST USER MESSAGE — rides the payload to ANY configured endpoint, not just Anthropic's.
---
## Claim
As EXP-0001. Detection results (known-proxy? CN-lab-keyword? CN-timezone?)
are smuggled to Anthropic inside the "Today's date" system-prompt line by
substituting the apostrophe with ', ’, ʼ, or ʹ — visually
identical, machine-readable.

## Evidence
EXP-0001: decoded lists + exact code excerpt, artifact sha256-pinned.
## Notes
Refines CLM-0003 (which described the mechanism less precisely, from press).
The channel is OUTBOUND via prompt text (server reads it), not "exfiltration
to a third party" — precision matters. Whether ANY code path also gates
features on these bits locally remains to be checked (grep for Zi_( callers).

## Update 2026-08-26 (EXP-0002)
Confirmed byte-equivalently in the 2.1.196 linux-x64 native binary (same Uup/Fup blobs, 147 hosts, 11 keywords, XOR-91). REMOVED in 2.1.246 → [[CLM-0012]].
