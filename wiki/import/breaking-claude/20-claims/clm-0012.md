---
id: CLM-0012
type: claim
grade: 3
status: verified
first_seen: 2026-08-26
last_confirmed: 2026-08-26
versions: ["2.1.246"]
sources: []
experiments: [EXP-0002]
superseded_by: null
summary: The 2.1.91/2.1.196 proxy-detection date-line steganography is REMOVED in v2.1.246 — currentDate is a plain `Today's date is ${V5()}.` with V5 wrapping a vanilla YYYY-MM-DD formatter; zero detection markers in the binary's plaintext app JS.
---
## Claim
In v2.1.246 (linux-x64 native binary): the "Today's date" line carries no
apostrophe-variant encoding, no CN-TZ date rewrite, and no host-blocklist /
lab-keyword / TZ detection feeding it. The chain
`currentDate → V5 = $h(Jrt) → Jrt()` produces plain `YYYY-MM-DD` with no
host or timezone input anywhere in the chain.

Two-tier precision:
- Grade **4** (DEMONSTRATED, EXP-0002): the date-line steganography channel
  ([[CLM-0011]]) is absent — chain fully resolved in artifact plaintext.
- Grade **3** (DOCUMENTED): no proxy-detection code anywhere in 2.1.246 —
  all telltales absent from plaintext app JS, but the container's
  zstd-magic residue was not fully decompressed (see EXP-0002 caveats).

## Evidence
- EXP-0002: code excerpts (`Ola` gone; `Jrt` plain formatter; currentDate
  template with ASCII apostrophe), absence counts for all 147-host blocklist
  sample + markers, XOR-85–95 sweep = 0. sha256-pinned artifacts.
- Consistent with CLM-0003's post-exposure rollback claim (Jun 2026);
  2.1.196 (pre-rollback? at least retaining) vs 2.1.246 (removed).

## Notes
What would raise to 4 overall: decode the compressed module remainder of
the Bun container, or one dynamic run on a disposable pod (wallet-gated).
What would refute: a compressed module in 246 containing a re-obfuscated
detector writing to another prompt field — EXP-0003's job.
