---
id: SES-0003
type: session
role: DISASSEMBLER (delegated, pi-exp-0002 lane)
date: 2026-08-26
models: "glm-5.2 (operator, zai); no subagents"
cost_usd: 0
verdicts: 2
---
# SES-0003 — EXP-0002 delegated run
- Acquired + sha256-pinned linux-x64 native binaries 2.1.196/2.1.246 (npm, free).
- Static-only, host CPU: 2.1.196 = mechanism byte-equivalent to 2.1.91
  (CLM-0011 extended); 2.1.246 = date-line channel removed (CLM-0012 new).
- Pod NOT used (static closed both; PI wallet also $0.85 < $1 minimum —
  flagged for the human).
- Failures/dead ends: 246 zstd-magic frames resist naive decompression
  (3865 candidates, dict/corruption errors) — EXP-0003 target. `Vup`-era
  name-chasing in minified bundle cost time; `$h` binding unresolved (harmless).
- Environment artifact: shell hook prints "claude native binary not installed"
  on some Bash calls — local npm wrapper, no target binary executed.
- Metrics: M3 versions analyzed 2→4 (88,91,196,246); M2 verdicts +2;
  M4 n/a (static reproductions, re-runnable from artifacts).
