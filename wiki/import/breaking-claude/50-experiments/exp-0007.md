---
id: EXP-0007
type: experiment
hypothesis: "The v2.1.88 harness consumes the remote policy-limits channel end-to-end: GETs /api/claude_code/policy_limits (initial + 1h background poll), parses the restrictions record, persists policy-limits.json, and enforcement decisions follow ONLY the served corrected keys (fail-open for unknown/missed); refuted keys (bypassPermissions/MCP) have no enforcement surface."
target_version: "2.1.88"
method: "dynamic (offline e2e against genuine held-tree service code)"
status: done
date: 2026-09-02
verdict: "supports (DEMONSTRATED g4 — offline behavioral confirmation; CLM-0020 stays g4)"
claims: [CLM-0020]
summary: "Fake localhost policy server + genuine held-tree v2.1.88 service: initial fetch + 1h poll observed, response parsed/cached/applied, corrected keys flip enforcement decisions at decision level, refuted keys have zero callsites (service itself is key-agnostic)."
---
## Hypothesis

As above (per SES-0006 brief objective).

## Method

- Kit: `experiments/exp-0007-killswitch/` (fake-policy-server.js: binds 127.0.0.1 only, zero deps, ETag/304 contract mirroring client checksum = sha256 over deep-sorted restrictions; variants/*.json; run-exp0007.sh; driver.ts).
- Driver imports the GENUINE held-tree `src/services/policyLimits/index.js` and calls its real entrypoints exactly as `src/main.tsx:958` does: `loadPolicyLimits()` then `startBackgroundPolling()`; decisions read via the real `isPolicyAllowed()`.
- Env matrix: `USER_TYPE=ant` + `USE_LOCAL_OAUTH=1` + `CLAUDE_LOCAL_OAUTH_API_BASE=http://127.0.0.1:<port>` routes the oauth `BASE_API_URL` to the fake server while `ANTHROPIC_BASE_URL` stays UNSET (required by `isPolicyLimitsEligible()` → `isFirstPartyAnthropicBaseUrl()`); fake `ANTHROPIC_API_KEY`; sandboxed `HOME`/`CLAUDE_CONFIG_DIR`; `DISABLE_TELEMETRY/ERROR_REPORTING/AUTOUPDATER=1`, `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1`; runtime `MACRO` global (see DEC-0003).
- No Anthropic egress: no inference calls, telemetry/updater disabled, only HTTP traffic is the policy GET to loopback.
- Deviations from shipped binary (documented, DEC-0003): npm no longer serves 2.1.88 (2.1.87→2.1.89 gap) so the leak-tree SOURCE runs under bun; `USER_TYPE` evaluated at runtime (shipped builds inline `"external"`); ~250 stripped ant-internal modules stubbed as inert soft-proxies (none on the policy path); full-CLI boot out of scope — channel-level e2e.

## Expected vs Observed

| # | Run | Variant | EXPECTED | OBSERVED |
|---|-----|---------|----------|----------|
| 1 | runA | corrected-and-refuted | initial GET 200; cache written incl. refuted keys stored; decisions follow served values | confirmed: server saw `GET /api/claude_code/policy_limits` UA `claude-code/2.1.88` + `x-api-key`; harness debug `Fetched successfully → Saved to … → Applied new restrictions successfully`; `policy-limits.json` = served record; decisions rc=true rs=true pf=false |
| 2 | runA2 | corrected-deny (rc/rs=false) | decision vector impossible under fail-open: rc=false rs=false | confirmed: rc=false rs=false pf=true — decision-level proof of remote consumption |
| 3 | runB | all-open (refuted keys ABSENT) | cache rewritten; unknown keys fail-open true | confirmed: all true; cache content switched with variant |
| 4 | runN | corrected-and-refuted :8643 | port-independence replication | confirmed (same vector as runA) |
| 5 | runN2 | none (no server) | fail-open + `ESSENTIAL_TRAFFIC_DENY_ON_MISS` for product_feedback | confirmed: rc=true rs=true pf=false refuted=true — the exact source semantics; also matches the pre-fix MACRO failure signature (5x retry → fail-open) |
| 6 | runC | corrected-and-refuted, 3840s | background poll at ~60min carrying `If-None-Match` → 304; debug `Using cached restrictions (304)` | CONFIRMED: poll #2 at exactly t=+3600s (22:57:02→23:57:02Z), `if_none_match: "sha256:92f2f78c…"` → served 304; harness debug `Using cached restrictions (304)` + `Cache still valid (304 Not Modified)`; decisions stable through exit |

KEY NUANCE (extends claim wording): `isPolicyAllowed(key)` honors ANY key present in the served record — the service is a generic record lookup. The v2.1.88 "no bypassPermissions/MCP killswitch" boundary is CALLSITE-level: no callsite queries those keys (EXP-0004 enumeration; e2e shows serving them changes nothing because nothing asks). Watch later versions for new callsites (Q-0006).

## Verdict

SUPPORTS CLM-0020 at DEMONSTRATED g4 (behavioral, offline). Channel real: endpoint, response schema, 1h background poll (observed at exactly +3600s), ETag/304 caching, 0600 cache file, fail-open + essential-traffic exception — all reproduced with genuine held-tree code. CLM-0014 stays superseded: refuted keys have no enforcement surface in v2.1.88.

## Raw logs

- `archive/exp-0007-killswitch-2026-09-02/runs/{runA,runA2,runB,runN,runN2-no-server,runC-1h-poll}/` — per-run: `requests.jsonl` (server side), `harness.log` (driver stdout), `harness-debug-logs/` (harness `logForDebugging` output), `policy-limits.snapshot.json` + `.sha256`, `run-manifest.json`
- `archive/exp-0007-killswitch-2026-09-02/SHA256SUMS`
- Kit: `experiments/exp-0007-killswitch/` (README has run matrix + deviations; `leak-tree-gap-build.log` = 250 unresolved modules evidence)
