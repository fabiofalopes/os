# DECISIONS LOG

Format: date — decision — why. Append-only. Reversals allowed but must be logged.

---

2026-08-27 — **Foundry created** at `~/foundry/`. Separate from the vault (`~/obsidian-vault-kali`) because it is a *workshop*: working files, datasets, render outputs. Finished canon/grammar/principles get mirrored into the vault as the book. The vault stays curated; the foundry stays messy-but-organized. — *Why: the vault is the product; a product can't live in a workshop.*

2026-08-27 — **101 series archived as `50-artifacts/reference-101/`** (word2vec 6 scenes + LLM 12 scenes, sources only). Designated the slop-floor: predictable-in-shape, hand-placed data, flat grammar. Every future artifact must be unrecognizable against it. — *Why: the user predicted its shape pre-generation; that calibrated the quality gate.*

2026-08-27 — **Phase locked to G1 (canon).** No rendering authorized until 20-grammar/ survives user review. — *Why: cadence before content; grammar is the compounding asset.*

2026-08-27 — **Orchestration prompt lives at `00-manifest/ORCHESTRATION.md`** and is the entry point for every new session/pane working on this project. — *Why: single source of truth for the goal tree and protocol.*

2026-08-27 — **Palette from the 101 series adopted as Grammar v0** (semantic colors: blue=tokens/objects, purple=weights/matrices, green=results, orange=attention/heat, near-black bg). v0 is the floor to evolve from, not the standard. — *Why: consistency across artifacts compounds; start from what's proven.*

2026-08-27 — **Pattern extracted upward**: the general ontology of vault-driven foundries now lives at `~/vaultcraft/` (the foundry of foundries). The Foundry remains instance #1 and reports domain learnings to `vaultcraft/40-domains/animation-foundry.md`. — *Why: the method only compounds if written outside any one domain.*
