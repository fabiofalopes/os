# Session 2026-08-27 — Birth of the Foundry

## What happened
- Manim 0.21 installed rootless (pango/cairo/pycairo built against `~/pango-local` prefix; launch via `~/bin/manim`).
- Produced the 101 series: word2vec (6 scenes) + How-an-LLM-works (12 scenes incl. KV-cache, context-memory, emergence, heartbeat). LAN gallery at `192.168.108.200:8931`.
- User recognized the output's shape *before* generation → slop-detector calibrated → the whole project reframed.
- Orchestration prompt written → now `00-manifest/ORCHESTRATION.md`.
- Foundry directory created; methodology encoded in README + DECISIONS.

## What was learned (principles worth keeping)
1. One-shot 101 content is fully predictable; predictability is the slop metric.
2. Expert tier = invented grammar + real data + matching viewer. All three.
3. Manim API traps are real and catalogued (font_size ctor-only, Brace.get_text, Matrix kwargs, aligned_edge, .set_opacity, empty AnimationGroup).
4. The interesting seam of LLM content: training internals → inference mechanics → serving economics. KV-caching-as-concept resonated most with the user.

## What the next artist needs
- Phase is G1: fill `10-canon/` using `_template-concept.md`. Start: residual stream, attention head geometry, superposition, QK/OV circuits, KV-cache economics, speculative decoding, quantization deformation, continuous batching.
- Do NOT render anything. Grammar (G2) doesn't exist yet.
- GPU ground-truth run (G3) can be prepared in parallel — spec it in `30-ground-truth/`.
