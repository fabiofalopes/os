---
tags: [critic, gate, quant, momentum, precommitment, review, ledger-row-3]
date: 2026-07-26
role: Critic
status: reviewed — 8 amendments via in-place hardening of [[quant-pilot-01]]; frozen kill criteria (thresholds/windows/grid/costs) untouched; pilot survives; Curator: link from pilot note (done in amendment) + INDEX entry
related:
  - "[[quant-pilot-01]]"
  - "[[López de Prado — Backtest Overfitting Guards]]"
  - "[[ledger]]"
  - "[[critic-forecast-pilot-01-2026-07-22]]"
  - "[[Operating Principle — Test Don't Wonder]]"
---

# Critic — Adversarial Review of Quant Pilot 01 (Pre-Execution Hardening)

> **Mandate:** attack [[quant-pilot-01]] (row 3, pre-registered 2026-07-23, never run) on four surfaces before the EXECUTE job — (1) is the free data point-in-time and survivorship-free? (2) is the OOS split honest (no peeking)? (3) does the PBO criterion bind — PBO on a single config is degenerate, per [[López de Prado — Backtest Overfitting Guards]] it needs a config family / CSCV? (4) is the frozen 12-1 momentum baseline a fair test or a straw man? Kill or fix each failing item; amend the note in place WITHOUT moving the frozen kill criterion.

## Blindness certification (test, don't wonder)

The pilot's exact statistic has **never been computed.** Verified 2026-07-26: no `quant-pilot-01-RESULT.md` exists; `~/Projects/trading-agents/quant-research/data/raw` holds only FRED macro series + 7 ETF/mega-cap CSVs + crypto — **no constituent panel was ever fetched**, no quantile/decile code path exists, `run_backtests.py` has no train/OOS/grid structure. **Disclosure (honesty over optics):** this review did read the related-design headline in that repo's `RESULTS.md` (3-asset XS momentum, Sharpe 0.55, 2016-07→2026-07 — see F3). That is prior evidence *in the KILL direction* on a *different* statistic; the pilot's own verdict remains unobserved, and the prior is now disclosed inside the note (A8).

## Evidence base

- **Vault forensics:** `ls wiki/value/` → no RESULT note; the pilot note is uncommitted (empty `git log` for its path) — consistent with 07-23 pre-registration, never executed.
- **Repo forensics:** quant-research `data/raw` = FRED-derived SPX/TBILL/UST10 + macro + 7 equities (SPY/QQQ/IWM/AAPL/MSFT/JPM/AMZN/GOOGL) + crypto. Its `xs_momentum` ran as **3-asset rotation** (top-1 + risk-off overlay, 5 bps, FRED *price* index — ex-dividend, their own caveat), not 500-name cross-section.
- **Arithmetic (independently recomputed):** train = 60, val = 36, OOS = 42, full = **138 months**; **138 mod 16 = 10** (CSCV blocks don't divide); trim → 128 = 16×8, **C(16,8) = 12,870**; grid 3×3×2 = **18 = trials ✓**; DSR expected-max-SR coefficient (N=18) = 1.854, σ_SR ≈ 1/√42 = 0.154/mo → null threshold ≈ 0.286/mo ≈ **0.99 annualized** (normal null; realized skew/kurt raise it); cost drag @ 40% monthly TO × 10 bps/side ≈ **1.0%/yr**.
- **Dates:** OOS end **2026-06-30 < pre-registration 2026-07-23** (frontmatter) → the end date could not have been chosen on results.

## Findings (each with verdict)

**F1 — Data is NOT point-in-time (surface 1). Verdict: AMEND (A1+A2), not fatal.** Survivorship is disclosed ✓, but step 1 labels the frozen list "point-in-time membership" — a mislabel (point-in-time = membership as of each historical date; the current list is the opposite). A second bias goes unlogged: names added to the index after 2015 are backtested before their addition date (**look-ahead**, same inflating direction). The directional claim "inflates → KILL conservative" is too strong: 10 bps/side ≈ 2× true large-cap friction (≈1%/yr drag) *deflates*, and premium-M&A delistings remove some winners — net believed inflating (failure-delisting channel dominates) but not guaranteed. **Why not fatal:** the pilot is KILL-oriented; a clear KILL survives any plausible net bias, and PROMOTE already carries caveat + Critic + DSR/PBO. Fix: honest "NOT point-in-time" label, look-ahead logged, bias-offset logged with a marginal-KILL (`SR_X ∈ [−0.3, 0]`, gross-positive) "killed by costs" reading rule — interpretation, never revival.

**F2 — OOS split is mechanically honest; two executor-discretion vectors closed (surface 2a). Verdict: PASS + AMEND (A3+A4).** End date predates pre-registration (above); config selection on train only; validation logged, never re-selected on; single-execution-run clause; data start 2014-01-01 covers the first formation window; guard-first ordering, and INCONCLUSIVE cannot manufacture a PROMOTE (PROMOTE requires N≥30 independently). ✓ But two unspecified mechanical details = post-hoc degrees of freedom = peeking vectors: **(a)** the full series is 138 months, **not divisible by 16 CSCV blocks** — the executor would improvise the trim. **Frozen: first 128 months (2015-01→2025-08), 16×8, 12,870 combos**; the SR_X verdict window is unaffected (full 42-mo OOS); PBO over the full (trimmed) matrix is paper-standard multiple-testing correction, not a second OOS test. **(b)** holding H∈{2,3} construction unspecified (6 of 18 configs ambiguous: overlapping-cohort average vs single cohort). **Frozen: equal-weight average of the H most recent monthly cohorts** (standard Jegadeesh–Titman overlapping construction).

**F3 — Prior observation: the no-peeking claim was incomplete (surface 2b — the sharpest finding). Verdict: AMEND (A8), not kill.** `~/Projects/trading-agents/quant-research/backtests/RESULTS.md` (dated 2026-07-23, same week as pre-registration) reports **XS 12-1 momentum — the same factor family — over 2016-07-25→2026-07-22, a window that fully contains the frozen OOS window**: raw Sharpe 0.55, losing to 60/40 (0.79) and SPX buy-and-hold. The pilot note never mentioned it. **Why it is not the pilot's statistic:** universe = 3 macro assets, not ~500 stocks; construction = top-1 + risk-off overlay, not quantile long-only; data = FRED price index (ex-dividend, crude), not adjusted OHLCV; costs 5 vs 10 bps; metric = raw Sharpe, not SR_X vs equal-weight; no grid, no DSR/PBO; verified no constituent data fetched. **Why amend, not kill:** the exact falsification statistic is uncomputed, and the prior points toward KILL — the thing peeking risk is a false PROMOTE, which the mechanical gates (DSR AND PBO AND Critic) still block. Per [[López de Prado — Backtest Overfitting Guards]] guard #4 ("the researcher has seen the holdout period"), the correct remedy is disclosure: the note now logs the prior (trials=18 counts the grid only; prior run logged, not hidden) and the RESULT note must explicitly reconcile its verdict against those RESULTS.md numbers.

**F4 — PBO binds; the degenerate case does NOT apply (surface 3). Verdict: PASS + caveat (A5).** The job's stated concern — PBO on a single config is degenerate — is checked and **cleared**: the grid is 3×3×2 = 18 configs = the DSR trial count ✓, and CSCV across the 18-config P&L matrix is exactly the paper's setup (the mechanical 138∤16 defect is fixed by A3). Residual, now logged: the grid is a tightly correlated family (overlapping formation windows, same universe/dates), and under high cross-config correlation the IS-best tracks the OOS-best, so **PBO is biased optimistic**; trials=18 also excludes the published factor zoo (mitigated: the factor family was chosen from theory — Jegadeesh–Titman 1993 — not in-sample search). `PBO < 0.3` is therefore **necessary-not-sufficient**; the gate composition carries the load.

**F5 — Baseline is not a straw man; one fairness flaw tightened (surface 4). Verdict: PASS + AMEND (A7).** The straw-man reading ("test a decayed factor, declare quant dead") is blocked by the note's own scoped KILL consequence — killed *as stated* = the classical-factor path on free large-cap data, revival only as a **new** row (the news/filings channel [[ktd-fin]] flags) — plus the rung-0 dual purpose: any OOS number becomes the baseline every future LLM signal must beat. A KILL yields evidence + validated infra, not a ritual. **Fairness flaw:** the equal-weight benchmark in the mega-cap-concentrated 2023–2026 window — a momentum long book of mega-cap winners can post positive SR_X vs EW off the cap-vs-EW spread alone (beta-as-alpha). The note logged attribution as partial and SPY as a *secondary* diagnostic, but PROMOTE didn't bind on it. **Fix (tightens PROMOTE; KILL row untouched):** SR_X vs SPY is now a mandatory reported diagnostic, and SR_X(EW) passing while SR_X(SPY) ≤ 0 fires the PROMOTE row's existing "Critic fails to refute" clause.

**F6 — Test-power honesty. Verdict: AMEND (A6).** Recomputed: at T = 42 months, N = 18 trials, the DSR null threshold ≈ 1.85·σ_SR ≈ **SR_X ≈ 1.0 annualized** (higher once realized negative skew/fat tails enter — executor recomputes). The `SR_X ≥ 0.5` line is slack; DSR dominates. PROMOTE is a high bar by construction and **NO-EVIDENCE is the modal expected outcome** — now stated in the note so it reads as "underpowered to confirm," not "edge absent" (mirrors forecast-pilot Critic F4/F7).

**F7 — Arithmetic slip in the source clip (Curator flag, not amended here).** [[López de Prado — Backtest Overfitting Guards]] says "S=16 → 12,780 combos"; **C(16,8) = 12,870**. The pilot amendments use the correct value. Wiki note = Z2 — flagged for Curator, not touched in a Critic pass.

**F8 — Constructive: execution-fidelity infra already exists.** The quant-research harness has 12 passing known-answer checks (buy&hold to 1e-15; exact cost model; no-lookahead proven via same-bar signal ≈ 0 under a 1-bar lag). The executor should **extend it**, not write a new backtester. Two flags: that repo's run hit **yfinance rate-limiting on the shared IP** — the stooq fallback and INCONCLUSIVE guard are live risks, not boilerplate; and its SPX series is a **price index** (ex-dividend) — unusable for SR_X, fetch adjusted constituent OHLCV fresh.

## Verdict

**PILOT SURVIVES — hardened.** Data: honestly NOT point-in-time now, bias direction logged with its offsets (F1). OOS split: mechanically honest, two discretion vectors frozen, and the undisclosed prior observation disclosed per de Prado guard #4 (F2/F3). PBO: binds — the degenerate single-config case does not apply; optimism caveats logged (F4). Baseline: not a straw man; beta-as-alpha path closed (F5). Plus power honesty (F6) and executor-facing infra flags (F8). **8 in-place amendments; the frozen kill criterion — thresholds, windows, grid, costs — is byte-identical.** Most-likely outcome remains KILL or NO-EVIDENCE (by construction: decayed factor + DSR power at T=42), and that is the point: row 3's fate is a real test, now with its priors on the table.
